# 摩塔 Mota - 重构实施方案

> 版本：1.0  
> 日期：2025-12-27  
> 文档类型：技术实施方案

---

## 1. 重构概述

### 1.1 重构目标

基于《重构vs重建决策分析》文档的推荐方案，采用**核心模块重写 + 其他模块复用**的策略，重点解决以下问题：

1. **代码质量问题**：`projects/index.tsx` 达到 3108 行，严重违反单一职责原则
2. **状态管理混乱**：单个组件内有 30+ 个 useState，状态分散难以维护
3. **核心流程不通**：项目 → 里程碑 → 部门任务 → 执行任务的流程无法顺畅运行

### 1.2 重构范围

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              重构范围                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✅ 需要重写的模块                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 项目管理模块 (modules/project/)                                  │   │
│  │  • 里程碑管理模块 (modules/milestone/)                              │   │
│  │  • 任务管理模块 (modules/task/)                                     │   │
│  │  • 统一状态管理 (Zustand stores)                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ⏸️ 保持不变的模块                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 认证模块 (auth/)                                                 │   │
│  │  • AI模块 (ai/)                                                     │   │
│  │  • 知识库模块 (knowledge/)                                          │   │
│  │  • 通知模块 (notifications/)                                        │   │
│  │  • 日历模块 (calendar/)                                             │   │
│  │  • 报表模块 (report-analytics/)                                     │   │
│  │  • 系统设置模块 (settings/, system/)                                │   │
│  │  • 组织管理模块 (departments/, members/)                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 新架构设计

### 2.1 目录结构

```
mota-user/src/
├── modules/                          # 🆕 新增：模块化组织
│   ├── project/                      # 项目模块
│   │   ├── components/               # 项目相关组件
│   │   │   ├── ProjectCard/
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.module.css
│   │   │   ├── ProjectForm/
│   │   │   │   ├── index.tsx
│   │   │   │   ├── BasicInfoStep.tsx
│   │   │   │   ├── MilestoneStep.tsx
│   │   │   │   └── index.module.css
│   │   │   ├── ProjectDetail/
│   │   │   │   ├── index.tsx
│   │   │   │   ├── OverviewTab.tsx
│   │   │   │   ├── TasksTab.tsx
│   │   │   │   ├── MembersTab.tsx
│   │   │   │   └── index.module.css
│   │   │   ├── ProjectSettings/
│   │   │   │   ├── index.tsx
│   │   │   │   ├── GeneralSettings.tsx
│   │   │   │   ├── NotificationSettings.tsx
│   │   │   │   ├── PermissionSettings.tsx
│   │   │   │   └── index.module.css
│   │   │   └── ProjectList/
│   │   │       ├── index.tsx
│   │   │       ├── GridView.tsx
│   │   │       ├── ListView.tsx
│   │   │       ├── Toolbar.tsx
│   │   │       └── index.module.css
│   │   ├── hooks/                    # 项目相关 hooks
│   │   │   ├── useProject.ts
│   │   │   ├── useProjectList.ts
│   │   │   └── useProjectMutation.ts
│   │   ├── store/                    # 项目状态管理
│   │   │   └── projectStore.ts
│   │   ├── services/                 # 项目 API（复用现有）
│   │   │   └── index.ts
│   │   └── types/                    # 类型定义
│   │       └── index.ts
│   │
│   ├── milestone/                    # 里程碑模块
│   │   ├── components/
│   │   │   ├── MilestoneTimeline/
│   │   │   ├── MilestoneForm/
│   │   │   └── MilestoneCard/
│   │   ├── hooks/
│   │   │   ├── useMilestone.ts
│   │   │   └── useMilestoneList.ts
│   │   ├── store/
│   │   │   └── milestoneStore.ts
│   │   └── types/
│   │       └── index.ts
│   │
│   └── task/                         # 任务模块
│       ├── components/
│       │   ├── TaskTree/             # 任务层级树
│       │   ├── TaskKanban/           # 看板视图
│       │   ├── TaskDetail/           # 任务详情
│       │   ├── TaskForm/             # 任务表单
│       │   ├── TaskDecompose/        # AI任务分解
│       │   └── TaskAssignment/       # 智能分工
│       ├── hooks/
│       │   ├── useTask.ts
│       │   ├── useTaskList.ts
│       │   └── useTaskMutation.ts
│       ├── store/
│       │   └── taskStore.ts
│       └── types/
│           └── index.ts
│
├── shared/                           # 🆕 新增：共享资源
│   ├── components/                   # 通用组件
│   │   ├── StatusTag/
│   │   ├── PriorityTag/
│   │   ├── UserAvatar/
│   │   ├── ProgressBar/
│   │   └── EmptyState/
│   ├── hooks/                        # 通用 hooks
│   │   ├── useDebounce.ts
│   │   ├── useLocalStorage.ts
│   │   └── usePagination.ts
│   ├── utils/                        # 工具函数
│   │   ├── date.ts
│   │   ├── format.ts
│   │   └── validation.ts
│   └── types/                        # 通用类型
│       └── common.ts
│
├── pages/                            # 页面组件（简化为路由入口）
│   ├── projects/
│   │   ├── index.tsx                 # 项目列表页（简化）
│   │   ├── create/
│   │   │   └── index.tsx             # 创建项目页
│   │   └── [id]/
│   │       ├── index.tsx             # 项目详情页
│   │       └── settings/
│   │           └── index.tsx         # 项目设置页
│   └── tasks/
│       ├── index.tsx                 # 任务列表页
│       └── [id]/
│           └── index.tsx             # 任务详情页
│
├── components/                       # 保留现有通用组件
├── layouts/                          # 保留现有布局
├── services/                         # 保留现有 API 服务
├── store/                            # 保留现有全局状态
└── styles/                           # 保留现有样式
```

### 2.2 状态管理设计

#### 2.2.1 项目状态管理 (projectStore.ts)

```typescript
// modules/project/store/projectStore.ts
import { create } from 'zustand'
import { devtools, persist } from 'zustand/middleware'
import { immer } from 'zustand/middleware/immer'
import * as projectApi from '@/services/api/project'
import type { Project, ProjectMember, Milestone, ProjectStatistics } from '@/services/api/project'

interface ProjectState {
  // ==================== 状态 ====================
  projects: Project[]
  currentProject: Project | null
  projectMembers: ProjectMember[]
  projectMilestones: Milestone[]
  projectStats: ProjectStatistics | null
  
  // 加载状态
  loading: boolean
  detailLoading: boolean
  error: string | null
  
  // 筛选和视图
  filters: {
    status: string
    search: string
    starred: boolean
  }
  viewMode: 'grid' | 'list' | 'gantt' | 'calendar' | 'kanban'
  
  // ==================== 操作 ====================
  // 项目列表
  fetchProjects: () => Promise<void>
  setFilters: (filters: Partial<ProjectState['filters']>) => void
  setViewMode: (mode: ProjectState['viewMode']) => void
  
  // 项目详情
  fetchProjectDetail: (id: string) => Promise<void>
  setCurrentProject: (project: Project | null) => void
  
  // 项目 CRUD
  createProject: (data: projectApi.CreateProjectRequest) => Promise<Project>
  updateProject: (id: string, data: projectApi.UpdateProjectRequest) => Promise<void>
  deleteProject: (id: string) => Promise<void>
  
  // 项目操作
  toggleStar: (id: string) => Promise<void>
  archiveProject: (id: string) => Promise<void>
  restoreProject: (id: string) => Promise<void>
  
  // 成员管理
  fetchProjectMembers: (projectId: string) => Promise<void>
  addMember: (projectId: string, userId: string, role?: string) => Promise<void>
  removeMember: (projectId: string, userId: string) => Promise<void>
  
  // 里程碑管理
  fetchProjectMilestones: (projectId: string) => Promise<void>
  addMilestone: (projectId: string, milestone: Partial<Milestone>) => Promise<void>
  updateMilestone: (milestoneId: string, data: Partial<Milestone>) => Promise<void>
  deleteMilestone: (milestoneId: string) => Promise<void>
  
  // 乐观更新
  optimisticUpdate: (id: string, data: Partial<Project>) => void
  rollback: (id: string, originalData: Project) => void
  
  // 重置
  reset: () => void
}

const initialState = {
  projects: [],
  currentProject: null,
  projectMembers: [],
  projectMilestones: [],
  projectStats: null,
  loading: false,
  detailLoading: false,
  error: null,
  filters: {
    status: 'all',
    search: '',
    starred: false
  },
  viewMode: 'grid' as const
}

export const useProjectStore = create<ProjectState>()(
  devtools(
    persist(
      immer((set, get) => ({
        ...initialState,
        
        // ==================== 项目列表 ====================
        fetchProjects: async () => {
          set({ loading: true, error: null })
          try {
            const { filters } = get()
            const res = await projectApi.getProjects({
              status: filters.status === 'all' ? undefined : filters.status,
              search: filters.search || undefined
            })
            set({ projects: res.list || [], loading: false })
          } catch (error: any) {
            set({ error: error.message, loading: false })
          }
        },
        
        setFilters: (newFilters) => {
          set((state) => {
            state.filters = { ...state.filters, ...newFilters }
          })
        },
        
        setViewMode: (mode) => {
          set({ viewMode: mode })
        },
        
        // ==================== 项目详情 ====================
        fetchProjectDetail: async (id) => {
          set({ detailLoading: true, error: null })
          try {
            const [project, members, milestones, stats] = await Promise.all([
              projectApi.getProjectById(id),
              projectApi.getProjectMembers(id).catch(() => []),
              projectApi.getProjectMilestones(id).catch(() => []),
              projectApi.getProjectStatistics(id).catch(() => null)
            ])
            set({
              currentProject: project,
              projectMembers: members,
              projectMilestones: milestones,
              projectStats: stats,
              detailLoading: false
            })
          } catch (error: any) {
            set({ error: error.message, detailLoading: false })
          }
        },
        
        setCurrentProject: (project) => {
          set({ currentProject: project })
        },
        
        // ==================== 项目 CRUD ====================
        createProject: async (data) => {
          set({ loading: true, error: null })
          try {
            const project = await projectApi.createProjectFull(data)
            set((state) => {
              state.projects.unshift(project)
              state.loading = false
            })
            return project
          } catch (error: any) {
            set({ error: error.message, loading: false })
            throw error
          }
        },
        
        updateProject: async (id, data) => {
          const { projects } = get()
          const originalProject = projects.find(p => p.id === id)
          
          // 乐观更新
          if (originalProject) {
            get().optimisticUpdate(id, data)
          }
          
          try {
            await projectApi.updateProject(id, data)
            // 更新成功，刷新列表
            await get().fetchProjects()
          } catch (error: any) {
            // 回滚
            if (originalProject) {
              get().rollback(id, originalProject)
            }
            set({ error: error.message })
            throw error
          }
        },
        
        deleteProject: async (id) => {
          try {
            await projectApi.deleteProject(id)
            set((state) => {
              state.projects = state.projects.filter(p => p.id !== id)
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        // ==================== 项目操作 ====================
        toggleStar: async (id) => {
          const { projects } = get()
          const project = projects.find(p => p.id === id)
          if (!project) return
          
          // 乐观更新
          set((state) => {
            const p = state.projects.find(p => p.id === id)
            if (p) {
              p.starred = p.starred === 1 ? 0 : 1
            }
          })
          
          try {
            await projectApi.toggleProjectStar(id)
          } catch (error: any) {
            // 回滚
            set((state) => {
              const p = state.projects.find(p => p.id === id)
              if (p) {
                p.starred = project.starred
              }
            })
            throw error
          }
        },
        
        archiveProject: async (id) => {
          try {
            await projectApi.archiveProject(id)
            set((state) => {
              const p = state.projects.find(p => p.id === id)
              if (p) {
                p.status = 'archived'
              }
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        restoreProject: async (id) => {
          try {
            await projectApi.restoreProject(id)
            set((state) => {
              const p = state.projects.find(p => p.id === id)
              if (p) {
                p.status = 'active'
              }
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        // ==================== 成员管理 ====================
        fetchProjectMembers: async (projectId) => {
          try {
            const members = await projectApi.getProjectMembers(projectId)
            set({ projectMembers: members })
          } catch (error: any) {
            set({ error: error.message })
          }
        },
        
        addMember: async (projectId, userId, role) => {
          try {
            await projectApi.addProjectMember(projectId, userId, role)
            await get().fetchProjectMembers(projectId)
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        removeMember: async (projectId, userId) => {
          try {
            await projectApi.removeProjectMember(projectId, userId)
            set((state) => {
              state.projectMembers = state.projectMembers.filter(m => m.userId !== userId)
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        // ==================== 里程碑管理 ====================
        fetchProjectMilestones: async (projectId) => {
          try {
            const milestones = await projectApi.getProjectMilestones(projectId)
            set({ projectMilestones: milestones })
          } catch (error: any) {
            set({ error: error.message })
          }
        },
        
        addMilestone: async (projectId, milestone) => {
          try {
            const newMilestone = await projectApi.addMilestone(projectId, milestone)
            set((state) => {
              state.projectMilestones.push(newMilestone)
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        updateMilestone: async (milestoneId, data) => {
          try {
            await projectApi.updateMilestone(milestoneId, data)
            set((state) => {
              const idx = state.projectMilestones.findIndex(m => m.id === milestoneId)
              if (idx !== -1) {
                state.projectMilestones[idx] = { ...state.projectMilestones[idx], ...data }
              }
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        deleteMilestone: async (milestoneId) => {
          try {
            await projectApi.deleteMilestone(milestoneId)
            set((state) => {
              state.projectMilestones = state.projectMilestones.filter(m => m.id !== milestoneId)
            })
          } catch (error: any) {
            set({ error: error.message })
            throw error
          }
        },
        
        // ==================== 乐观更新 ====================
        optimisticUpdate: (id, data) => {
          set((state) => {
            const idx = state.projects.findIndex(p => p.id === id)
            if (idx !== -1) {
              state.projects[idx] = { ...state.projects[idx], ...data }
            }
            if (state.currentProject?.id === id) {
              state.currentProject = { ...state.currentProject, ...data }
            }
          })
        },
        
        rollback: (id, originalData) => {
          set((state) => {
            const idx = state.projects.findIndex(p => p.id === id)
            if (idx !== -1) {
              state.projects[idx] = originalData
            }
            if (state.currentProject?.id === id) {
              state.currentProject = originalData
            }
          })
        },
        
        // ==================== 重置 ====================
        reset: () => {
          set(initialState)
        }
      })),
      {
        name: 'project-store',
        partialize: (state) => ({
          filters: state.filters,
          viewMode: state.viewMode
        })
      }
    ),
    { name: 'ProjectStore' }
  )
)
```

#### 2.2.2 任务状态管理 (taskStore.ts)

```typescript
// modules/task/store/taskStore.ts
import { create } from 'zustand'
import { devtools } from 'zustand/middleware'
import { immer } from 'zustand/middleware/immer'
import * as taskApi from '@/services/api/task'
import * as departmentTaskApi from '@/services/api/departmentTask'
import type { Task, TaskStatus } from '@/services/api/task'
import type { DepartmentTask } from '@/services/api/departmentTask'

interface TaskState {
  // ==================== 状态 ====================
  // 部门任务
  departmentTasks: DepartmentTask[]
  currentDepartmentTask: DepartmentTask | null
  
  // 执行任务
  tasks: Task[]
  currentTask: Task | null
  
  // 加载状态
  loading: boolean
  error: string | null
  
  // 筛选
  filters: {
    projectId: string | null
    departmentTaskId: string | null
    status: string
    assigneeId: string | null
  }
  
  // ==================== 操作 ====================
  // 部门任务
  fetchDepartmentTasks: (projectId: string) => Promise<void>
  createDepartmentTask: (data: any) => Promise<DepartmentTask>
  updateDepartmentTask: (id: string, data: any) => Promise<void>
  deleteDepartmentTask: (id: string) => Promise<void>
  
  // 执行任务
  fetchTasks: (params?: taskApi.TaskQueryParams) => Promise<void>
  fetchTasksByDepartmentTask: (departmentTaskId: string) => Promise<void>
  fetchTasksByProject: (projectId: string) => Promise<void>
  createTask: (data: taskApi.CreateTaskRequest) => Promise<Task>
  updateTask: (id: string, data: taskApi.UpdateTaskRequest) => Promise<void>
  deleteTask: (id: string) => Promise<void>
  
  // 任务状态
  updateTaskStatus: (id: string, status: TaskStatus) => Promise<void>
  updateTaskProgress: (id: string, progress: number, note?: string) => Promise<void>
  completeTask: (id: string) => Promise<void>
  assignTask: (id: string, assigneeId: string) => Promise<void>
  
  // 筛选
  setFilters: (filters: Partial<TaskState['filters']>) => void
  
  // 重置
  reset: () => void
}

const initialState = {
  departmentTasks: [],
  currentDepartmentTask: null,
  tasks: [],
  currentTask: null,
  loading: false,
  error: null,
  filters: {
    projectId: null,
    departmentTaskId: null,
    status: 'all',
    assigneeId: null
  }
}

export const useTaskStore = create<TaskState>()(
  devtools(
    immer((set, get) => ({
      ...initialState,
      
      // ==================== 部门任务 ====================
      fetchDepartmentTasks: async (projectId) => {
        set({ loading: true, error: null })
        try {
          const tasks = await departmentTaskApi.getDepartmentTasksByProjectId(projectId)
          set({ departmentTasks: tasks || [], loading: false })
        } catch (error: any) {
          set({ error: error.message, loading: false })
        }
      },
      
      createDepartmentTask: async (data) => {
        set({ loading: true, error: null })
        try {
          const task = await departmentTaskApi.createDepartmentTask(data)
          set((state) => {
            state.departmentTasks.push(task)
            state.loading = false
          })
          return task
        } catch (error: any) {
          set({ error: error.message, loading: false })
          throw error
        }
      },
      
      updateDepartmentTask: async (id, data) => {
        try {
          await departmentTaskApi.updateDepartmentTask(id, data)
          set((state) => {
            const idx = state.departmentTasks.findIndex(t => t.id === id)
            if (idx !== -1) {
              state.departmentTasks[idx] = { ...state.departmentTasks[idx], ...data }
            }
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      deleteDepartmentTask: async (id) => {
        try {
          await departmentTaskApi.deleteDepartmentTask(id)
          set((state) => {
            state.departmentTasks = state.departmentTasks.filter(t => t.id !== id)
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      // ==================== 执行任务 ====================
      fetchTasks: async (params) => {
        set({ loading: true, error: null })
        try {
          const res = await taskApi.getTasks(params)
          set({ tasks: res.list || [], loading: false })
        } catch (error: any) {
          set({ error: error.message, loading: false })
        }
      },
      
      fetchTasksByDepartmentTask: async (departmentTaskId) => {
        set({ loading: true, error: null })
        try {
          const tasks = await taskApi.getTasksByDepartmentTaskId(departmentTaskId)
          set({ tasks: tasks || [], loading: false })
        } catch (error: any) {
          set({ error: error.message, loading: false })
        }
      },
      
      fetchTasksByProject: async (projectId) => {
        set({ loading: true, error: null })
        try {
          const tasks = await taskApi.getTasksByProjectId(projectId)
          set({ tasks: tasks || [], loading: false })
        } catch (error: any) {
          set({ error: error.message, loading: false })
        }
      },
      
      createTask: async (data) => {
        set({ loading: true, error: null })
        try {
          const task = await taskApi.createTask(data)
          set((state) => {
            state.tasks.push(task)
            state.loading = false
          })
          return task
        } catch (error: any) {
          set({ error: error.message, loading: false })
          throw error
        }
      },
      
      updateTask: async (id, data) => {
        try {
          await taskApi.updateTask(id, data)
          set((state) => {
            const idx = state.tasks.findIndex(t => t.id === id)
            if (idx !== -1) {
              state.tasks[idx] = { ...state.tasks[idx], ...data }
            }
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      deleteTask: async (id) => {
        try {
          await taskApi.deleteTask(id)
          set((state) => {
            state.tasks = state.tasks.filter(t => t.id !== id)
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      // ==================== 任务状态 ====================
      updateTaskStatus: async (id, status) => {
        const { tasks } = get()
        const originalTask = tasks.find(t => t.id === id)
        
        // 乐观更新
        set((state) => {
          const task = state.tasks.find(t => t.id === id)
          if (task) {
            task.status = status
          }
        })
        
        try {
          await taskApi.updateTaskStatus(id, status)
        } catch (error: any) {
          // 回滚
          if (originalTask) {
            set((state) => {
              const task = state.tasks.find(t => t.id === id)
              if (task) {
                task.status = originalTask.status
              }
            })
          }
          set({ error: error.message })
          throw error
        }
      },
      
      updateTaskProgress: async (id, progress, note) => {
        try {
          await taskApi.updateTaskProgress(id, progress, note)
          set((state) => {
            const task = state.tasks.find(t => t.id === id)
            if (task) {
              task.progress = progress
              if (note) task.progressNote = note
            }
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      completeTask: async (id) => {
        try {
          await taskApi.completeTask(id)
          set((state) => {
            const task = state.tasks.find(t => t.id === id)
            if (task) {
              task.status = taskApi.TaskStatus.COMPLETED
              task.progress = 100
            }
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      assignTask: async (id, assigneeId) => {
        try {
          await taskApi.assignTask(id, assigneeId)
          set((state) => {
            const task = state.tasks.find(t => t.id === id)
            if (task) {
              task.assigneeId = assigneeId
            }
          })
        } catch (error: any) {
          set({ error: error.message })
          throw error
        }
      },
      
      // ==================== 筛选 ====================
      setFilters: (newFilters) => {
        set((state) => {
          state.filters = { ...state.filters, ...newFilters }
        })
      },
      
      // ==================== 重置 ====================
      reset: () => {
        set(initialState)
      }
    })),
    { name: 'TaskStore' }
  )
)
```

---

## 3. 组件拆分方案

### 3.1 从 projects/index.tsx 拆分的组件

当前 `projects/index.tsx` 有 3108 行，需要拆分为以下独立组件：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  projects/index.tsx (3108行) 拆分方案                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  原文件功能                          拆分后组件                              │
│  ─────────────────────────────────────────────────────────────────────────  │
│  项目卡片渲染 (约200行)         →   ProjectCard/index.tsx (~150行)          │
│  项目列表视图 (约300行)         →   ProjectList/GridView.tsx (~100行)       │
│                                     ProjectList/ListView.tsx (~100行)       │
│                                     ProjectList/Toolbar.tsx (~80行)         │
│  创建项目抽屉 (约500行)         →   ProjectForm/index.tsx (~200行)          │
│                                     ProjectForm/BasicInfoStep.tsx (~150行)  │
│                                     ProjectForm/MilestoneStep.tsx (~150行)  │
│  编辑项目抽屉 (约200行)         →   ProjectForm/index.tsx (复用)            │
│  项目设置抽屉 (约400行)         →   ProjectSettings/index.tsx (~150行)      │
│                                     ProjectSettings/GeneralSettings.tsx     │
│                                     ProjectSettings/NotificationSettings.tsx│
│                                     ProjectSettings/PermissionSettings.tsx  │
│  项目详情抽屉 (约600行)         →   ProjectDetail/index.tsx (~200行)        │
│                                     ProjectDetail/OverviewTab.tsx (~150行)  │
│                                     ProjectDetail/TasksTab.tsx (~150行)     │
│                                     ProjectDetail/MembersTab.tsx (~100行)   │
│  甘特图视图 (约50行)            →   复用现有 GanttChart 组件                 │
│  日历视图 (约50行)              →   复用现有 Calendar 组件                   │
│  看板视图 (约50行)              →   复用现有 KanbanBoard 组件                │
│  分析视图 (约150行)             →   ProjectAnalytics/index.tsx (~150行)     │
│  归档视图 (约100行)             →   ProjectList/ArchivedView.tsx (~100行)   │
│  状态管理 (30+ useState)        →   projectStore.ts (Zustand)               │
│                                                                             │
│  拆分后 pages/projects/index.tsx 预计 ~150行（仅作为路由入口）              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 组件职责说明

| 组件 | 职责 | 预计行数 |
|------|------|----------|
| `ProjectCard` | 单个项目卡片展示，包含收藏、菜单操作 | ~150行 |
| `ProjectList/GridView` | 网格视图布局 | ~100行 |
| `ProjectList/ListView` | 列表视图布局 | ~100行 |
| `ProjectList/Toolbar` | 搜索、筛选、视图切换工具栏 | ~80行 |
| `ProjectForm` | 创建/编辑项目表单（步骤向导） | ~200行 |
| `ProjectForm/BasicInfoStep` | 基本信息步骤 | ~150行 |
| `ProjectForm/MilestoneStep` | 里程碑设置步骤 | ~150行 |
| `ProjectSettings` | 项目设置主组件 | ~150行 |
| `ProjectDetail` | 项目详情主组件 | ~200行 |
| `ProjectDetail/OverviewTab` | 概览标签页 | ~150行 |
| `ProjectDetail/TasksTab` | 任务标签页 | ~150行 |
| `ProjectDetail/MembersTab` | 成员标签页 | ~100行 |

---

## 4. 实施步骤

### 4.1 第一阶段：基础架构搭建（第1-2周）

#### Week 1: 目录结构和状态管理

**Day 1-2: 创建目录结构**
```bash
# 创建模块目录
mkdir -p mota-user/src/modules/project/{components,hooks,store,services,types}
mkdir -p mota-user/src/modules/milestone/{components,hooks,store,types}
mkdir -p mota-user/src/modules/task/{components,hooks,store,types}
mkdir -p mota-user/src/shared/{components,hooks,utils,types}
```

**Day 3-4: 实现 projectStore**
- 创建 `modules/project/store/projectStore.ts`
- 实现项目列表、详情、CRUD 操作
- 实现乐观更新和错误回滚

**Day 5: 实现 taskStore**
- 创建 `modules/task/store/taskStore.ts`
- 实现部门任务和执行任务管理
- 实现任务状态流转

#### Week 2: 共享组件和 Hooks

**Day 1-2: 共享组件**
```typescript
// shared/components/StatusTag/index.tsx
// shared/components/PriorityTag/index.tsx
// shared/components/UserAvatar/index.tsx
// shared/components/ProgressBar/index.tsx
// shared/components/EmptyState/index.tsx
```

**Day 3-4: 自定义 Hooks**
```typescript
// modules/project/hooks/useProject.ts
// modules/project/hooks/useProjectList.ts
// modules/project/hooks/useProjectMutation.ts
// modules/task/hooks/useTask.ts
// modules/task/hooks/useTaskList.ts
```

**Day 5: 类型定义整理**
```typescript
// modules/project/types/index.ts
// modules/task/types/index.ts
// shared/types/common.ts
```

### 4.2 第二阶段：项目管理模块重写（第3-4周）

#### Week 3: 项目列表和卡片

**Day 1-2: ProjectCard 组件**
```typescript
// modules/project/components/ProjectCard/index.tsx
```

**Day 3-4: ProjectList 组件**
```typescript
// modules/project/components/ProjectList/index.tsx
// modules/project/components/ProjectList/GridView.tsx
// modules/project/components/ProjectList/ListView.tsx
// modules/project/components/ProjectList/Toolbar.tsx
```

**Day 5: 集成测试**
- 验证项目列表功能
- 验证视图切换
- 验证筛选和搜索

#### Week 4: 项目表单和详情

**Day 1-2: ProjectForm 组件**
```typescript
// modules/project/components/ProjectForm/index.tsx
// modules/project/components/ProjectForm/BasicInfoStep.tsx
// modules/project/components/ProjectForm/MilestoneStep.tsx
```

**Day 3-4: ProjectDetail 组件**
```typescript
// modules/project/components/ProjectDetail/index.tsx
// modules/project/components/ProjectDetail/OverviewTab.tsx
// modules/project/components/ProjectDetail/TasksTab.tsx
// modules/project/components/ProjectDetail/MembersTab.tsx
```

**Day 5: ProjectSettings 组件**
```typescript
// modules/project/components/ProjectSettings/index.tsx
// modules/project/components/ProjectSettings/GeneralSettings.tsx
// modules/project/components/ProjectSettings/NotificationSettings.tsx
// modules/project/components/ProjectSettings/PermissionSettings.tsx
```

### 4.3 第三阶段：任务管理模块重写（第5-6周）

#### Week 5: 任务树和看板

**Day 1-2: TaskTree 组件**
```typescript
// modules/task/components/TaskTree/index.tsx
// 实现任务层级结构展示
// 项目 -> 里程碑 -> 部门任务 -> 执行任务 -> 子任务
```

**Day 3-4: TaskKanban 组件**
```typescript
// modules/task/components/TaskKanban/index.tsx
// 实现拖拽状态变更
// 实现任务卡片展示
```

**Day 5: 任务状态流转**
- 实现状态机逻辑
- 实现状态变更验证
- 实现进度自动计算

#### Week 6: 任务详情和表单

**Day 1-2: TaskDetail 组件**
```typescript
// modules/task/components/TaskDetail/index.tsx
// 任务信息展示
// 子任务管理
// 评论和附件
```

**Day 3-4: TaskForm 组件**
```typescript
// modules/task/components/TaskForm/index.tsx
// 创建/编辑任务
// 任务分配
// 依赖关系设置
```

**Day 5: 任务与里程碑关联**
- 实现里程碑下的任务分组
- 实现任务进度汇总到里程碑
- 实现里程碑完成度计算

### 4.4 第四阶段：AI功能集成（第7周）

**Day 1-2: AI任务分解**
```typescript
// modules/task/components/TaskDecompose/index.tsx
// 调用 AI 接口分解任务
// 展示分解结果
// 支持用户调整
```

**Day 3-4: 智能分工**
```typescript
// modules/task/components/TaskAssignment/index.tsx
// 分析成员技能和工作负载
// 推荐任务负责人
// 支持批量分配
```

**Day 5: AI建议展示**
- 集成 AI 建议到项目详情
- 实现风险预警展示
- 实现进度预测展示

### 4.5 第五阶段：测试和优化（第8周）

**Day 1-2: 端到端测试**
- 测试完整的项目创建流程
- 测试任务分解和分配流程
- 测试状态流转

**Day 3-4: 性能优化**
- 组件懒加载
- 列表虚拟滚动
- 状态更新优化

**Day 5: 文档和上线**
- 更新开发文档
- 代码审查
- 部署上线

---

## 5. 核心流程重新设计

### 5.1 项目创建流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  新项目创建流程                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Step 1: 基本信息                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 项目名称（必填）                                                  │   │
│  │  • 项目描述（选填）                                                  │   │
│  │  • 项目周期（必填）                                                  │   │
│  │  • 项目负责人（必填）                                                │   │
│  │  • 项目颜色（选填）                                                  │   │
│  │  • 项目附件（选填）                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                          │                                                  │
│                          ▼                                                  │
│  Step 2: 设置里程碑（可选）                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 手动添加里程碑                                                    │   │
│  │  • AI智能生成里程碑建议                                              │   │
│  │  • 拖拽排序里程碑                                                    │   │
│  │  • 设置里程碑负责人                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                          │                                                  │
│                          ▼                                                  │
│  完成创建 → 进入项目详情页                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 任务分解流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  AI任务分解流程                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  输入：项目目标和描述                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  用户输入项目目标，例如：                                            │   │
│  │  "开发一个电商小程序，包含商品展示、购物车、订单管理"                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                          │                                                  │
│                          ▼                                                  │
│  AI分析                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  1. 理解项目目标和范围                                               │   │
│  │  2. 检索类似历史项目（RAG）                                          │   │
│  │  3. 识别关键交付物和里程碑                                           │   │
│  │  4. 分解任务并估算工时                                               │   │
│  │  5. 识别任务依赖关系                                                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                          │                                                  │
│                          ▼                                                  │
│  输出：任务树结构                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📁 电商小程序开发                              预估: 45人天          │   │
│  │  ├── 🎯 里程碑1：需求分析完成                                        │   │
│  │  │   ├── 🏢 产品部任务                                               │   │
│  │  │   │   ├── 📋 需求调研                            2人天            │   │
│  │  │   │   └── 📋 需求文档                            1人天            │   │
│  │  │   └── 🏢 设计部任务                                               │   │
│  │  │       └── 📋 原型设计                            2人天            │   │
│  │  ├── 🎯 里程碑2：开发完成                                            │   │
│  │  │   ├── 🏢 前端组任务                                               │   │
│  │  │   └── 🏢 后端组任务                                               │   │
│  │  └── ...                                                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                          │                                                  │
│                          ▼                                                  │
│  用户确认和调整                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 调整任务名称、描述、工时                                          │   │
│  │  • 添加/删除/合并任务                                                │   │
│  │  • 调整任务依赖关系                                                  │   │
│  │  • 确认后自动创建所有任务                                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3 任务状态流转

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  任务状态流转                                                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  执行任务状态：                                                             │
│                                                                             │
│       ┌──────────┐                                                          │
│       │  待开始  │ ◄─────────────────────────────────────┐                  │
│       │ pending  │                                       │                  │
│       └────┬─────┘                                       │                  │
│            │ 开始任务                                    │ 重新开始         │
│            ▼                                             │                  │
│       ┌──────────┐         暂停          ┌──────────┐   │                  │
│       │  进行中  │ ──────────────────► │  已暂停  │ ──┘                  │
│       │in_progress│ ◄────────────────── │  paused  │                       │
│       └────┬─────┘         恢复          └──────────┘                       │
│            │                                                                │
│            │ 完成任务                                                       │
│            ▼                                                                │
│       ┌──────────┐                                                          │
│       │  已完成  │                                                          │
│       │completed │                                                          │
│       └──────────┘                                                          │
│                                                                             │
│  任何状态 ──────────────────────────────────► 已取消 (cancelled)            │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  进度自动计算规则：                                                         │
│  • 执行任务进度 → 汇总到部门任务进度                                        │
│  • 部门任务进度 → 汇总到里程碑进度                                          │
│  • 里程碑进度 → 汇总到项目进度                                              │
│                                                                             │
│  公式：父级进度 = Σ(子任务进度 × 子任务权重) / Σ(子任务权重)                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 风险控制

### 6.1 风险识别和应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 新旧代码集成问题 | 中 | 高 | 使用特性开关，逐步切换 |
| 开发进度延期 | 中 | 中 | 预留20%缓冲时间 |
| 功能遗漏 | 低 | 高 | 详细的功能对照清单 |
| 状态管理迁移问题 | 中 | 中 | 保留旧代码作为备份 |
| 性能下降 | 低 | 中 | 持续性能监控 |

### 6.2 回滚策略

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  回滚策略                                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 特性开关控制                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  // 在 .env 或配置中心设置                                           │   │
│  │  FEATURE_NEW_PROJECT_MODULE=true                                     │   │
│  │  FEATURE_NEW_TASK_MODULE=true                                        │   │
│  │                                                                       │   │
│  │  // 在代码中使用                                                      │   │
│  │  if (featureFlags.newProjectModule) {                                │   │
│  │    return <NewProjectList />                                         │   │
│  │  } else {                                                            │   │
│  │    return <OldProjectList />                                         │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  2. 代码保留策略                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 旧代码移动到 _legacy/ 目录，不删除                                │   │
│  │  • 新代码在 modules/ 目录开发                                        │   │
│  │  • 验证通过后再删除旧代码                                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  3. 数据兼容                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 数据库结构保持不变                                                │   │
│  │  • API 接口保持兼容                                                  │   │
│  │  • 状态结构保持兼容                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 验收标准

### 7.1 功能验收

| 功能 | 验收标准 |
|------|----------|
| 项目创建 | 2步完成创建，支持里程碑设置 |
| 项目列表 | 支持5种视图切换，筛选和搜索正常 |
| 项目详情 | 展示完整信息，支持编辑和设置 |
| 任务分解 | AI分解结果合理，支持用户调整 |
| 任务分配 | 智能推荐准确，支持手动调整 |
| 状态流转 | 状态变更正确，进度自动计算 |

### 7.2 代码质量验收

| 指标 | 标准 |
|------|------|
| 单文件行数 | ≤ 300行 |
| 组件 useState 数量 | ≤ 5个 |
| 类型覆盖率 | ≥ 90% |
| 代码重复率 | ≤ 5% |

### 7.3 性能验收

| 指标 | 标准 |
|------|------|
| 首屏加载时间 | ≤ 2秒 |
| 列表渲染时间 | ≤ 500ms |
| 状态更新响应 | ≤ 100ms |

---

## 8. 总结

本重构方案采用**核心模块重写 + 其他模块复用**的策略，重点解决项目管理和任务管理模块的代码质量问题和功能流程问题。

**关键改进：**
1. 模块化架构，单文件不超过300行
2. Zustand 统一状态管理，支持乐观更新
3. 组件职责单一，易于维护和测试
4. AI 功能集成，提升用户体验

**预期收益：**
- 代码可维护性提升 80%
- 开发效率提升 50%
- 用户操作步骤减少 50%
- 核心流程完整可用

---

*文档结束*