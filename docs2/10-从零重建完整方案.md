# 摩塔 Mota - 从零重建完整方案

> 版本：1.0  
> 日期：2025-12-27  
> 文档类型：重建实施方案

---

## 1. 项目概述

### 1.1 重建背景

当前系统存在以下根本性问题，需要从零重建：

| 问题类型 | 具体表现 | 影响程度 |
|----------|----------|----------|
| 架构问题 | 单文件3000+行，无模块化设计 | 严重 |
| 状态管理 | 30+ useState，状态分散混乱 | 严重 |
| 代码质量 | 高耦合、低内聚、难以测试 | 严重 |
| 用户体验 | 核心流程跑不通，操作繁琐 | 严重 |
| 技术债务 | 累积过多，修复成本高于重建 | 严重 |

### 1.2 重建目标

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              重建目标                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  🎯 核心目标                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  1. 打造流畅的项目协作体验                                          │   │
│  │     • 创建项目 → AI任务分解 → 智能分工 → 执行追踪 全流程打通        │   │
│  │     • 操作步骤减少60%，效率提升100%                                 │   │
│  │                                                                     │   │
│  │  2. 建立可扩展的技术架构                                            │   │
│  │     • 模块化设计，单一职责                                          │   │
│  │     • 统一状态管理，数据流清晰                                      │   │
│  │     • 组件复用率>80%                                                │   │
│  │                                                                     │   │
│  │  3. 实现AI深度融合                                                  │   │
│  │     • AI贯穿全流程，不是附加功能                                    │   │
│  │     • 智能推荐、自动化处理                                          │   │
│  │     • 企业知识资产化                                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  📊 量化指标                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  • 首屏加载时间 < 1.5s                                              │   │
│  │  • 页面切换时间 < 300ms                                             │   │
│  │  • 单文件代码行数 < 200行                                           │   │
│  │  • 测试覆盖率 > 80%                                                 │   │
│  │  • 组件复用率 > 80%                                                 │   │
│  │  • 用户操作步骤减少 60%                                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 项目周期

| 阶段 | 时间 | 内容 |
|------|------|------|
| 第一阶段 | 第1-4周 | 基础架构 + 核心协作流程 |
| 第二阶段 | 第5-8周 | AI功能 + 知识管理 |
| 第三阶段 | 第9-12周 | 辅助功能 + 优化上线 |

**总周期：12周（3个月）**

---

## 2. 技术架构

### 2.1 技术选型

#### 前端技术栈

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| React | 18.3+ | UI框架 | 生态成熟，团队熟悉 |
| TypeScript | 5.3+ | 类型系统 | 类型安全，开发效率 |
| Vite | 5.0+ | 构建工具 | 极速HMR，现代化 |
| Zustand | 4.5+ | 状态管理 | 轻量简洁，TypeScript友好 |
| TanStack Query | 5.0+ | 服务端状态 | 缓存、重试、乐观更新 |
| Ant Design | 5.12+ | UI组件库 | 企业级，功能完善 |
| React Router | 6.20+ | 路由 | 官方推荐，功能强大 |
| Tailwind CSS | 3.4+ | 样式方案 | 原子化，开发效率高 |
| Framer Motion | 10+ | 动画 | 声明式，性能好 |
| dnd-kit | 6.1+ | 拖拽 | 现代化，可访问性好 |

#### 后端技术栈

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| Spring Boot | 3.2+ | 后端框架 | 生态成熟，团队熟悉 |
| Java | 21 LTS | 开发语言 | 虚拟线程，性能提升 |
| MyBatis-Plus | 3.5+ | ORM | 简化开发，功能强大 |
| Spring Security | 6.2+ | 安全框架 | OAuth2，JWT |
| Redis | 7.2+ | 缓存 | 高性能，数据结构丰富 |
| MySQL | 8.0+ | 主数据库 | 稳定可靠 |
| Milvus | 2.3+ | 向量数据库 | RAG检索 |
| Elasticsearch | 8.11+ | 搜索引擎 | 全文检索 |

#### AI技术栈

| 技术 | 用途 | 说明 |
|------|------|------|
| LangChain4j | AI编排 | Java版LangChain |
| OpenAI API | 大模型 | GPT-4 Turbo |
| Claude API | 大模型 | Claude 3 |
| 通义千问 API | 大模型 | 国产备选 |
| Milvus | 向量存储 | 语义检索 |
| Neo4j | 图数据库 | 知识图谱 |

### 2.2 项目结构

```
mota-v2/
├── apps/                              # 应用目录
│   ├── web/                           # Web前端应用
│   │   ├── public/
│   │   ├── src/
│   │   │   ├── app/                   # 应用入口
│   │   │   │   ├── App.tsx
│   │   │   │   ├── providers.tsx      # 全局Provider
│   │   │   │   └── routes.tsx         # 路由配置
│   │   │   │
│   │   │   ├── modules/               # 业务模块
│   │   │   │   ├── auth/              # 认证模块
│   │   │   │   │   ├── components/
│   │   │   │   │   ├── hooks/
│   │   │   │   │   ├── pages/
│   │   │   │   │   ├── services/
│   │   │   │   │   ├── store/
│   │   │   │   │   └── types/
│   │   │   │   │
│   │   │   │   ├── project/           # 项目模块
│   │   │   │   │   ├── components/
│   │   │   │   │   │   ├── ProjectCard/
│   │   │   │   │   │   ├── ProjectList/
│   │   │   │   │   │   ├── ProjectForm/
│   │   │   │   │   │   ├── ProjectDetail/
│   │   │   │   │   │   └── ProjectSettings/
│   │   │   │   │   ├── hooks/
│   │   │   │   │   │   ├── useProject.ts
│   │   │   │   │   │   ├── useProjectList.ts
│   │   │   │   │   │   └── useProjectMutation.ts
│   │   │   │   │   ├── pages/
│   │   │   │   │   │   ├── ProjectListPage.tsx
│   │   │   │   │   │   ├── ProjectCreatePage.tsx
│   │   │   │   │   │   └── ProjectDetailPage.tsx
│   │   │   │   │   ├── services/
│   │   │   │   │   │   └── projectApi.ts
│   │   │   │   │   ├── store/
│   │   │   │   │   │   └── projectStore.ts
│   │   │   │   │   └── types/
│   │   │   │   │       └── project.ts
│   │   │   │   │
│   │   │   │   ├── task/              # 任务模块
│   │   │   │   │   ├── components/
│   │   │   │   │   │   ├── TaskTree/
│   │   │   │   │   │   ├── TaskKanban/
│   │   │   │   │   │   ├── TaskCard/
│   │   │   │   │   │   ├── TaskForm/
│   │   │   │   │   │   ├── TaskDetail/
│   │   │   │   │   │   ├── TaskDecompose/
│   │   │   │   │   │   └── TaskAssign/
│   │   │   │   │   ├── hooks/
│   │   │   │   │   ├── pages/
│   │   │   │   │   ├── services/
│   │   │   │   │   ├── store/
│   │   │   │   │   └── types/
│   │   │   │   │
│   │   │   │   ├── milestone/         # 里程碑模块
│   │   │   │   ├── ai/                # AI模块
│   │   │   │   │   ├── components/
│   │   │   │   │   │   ├── AIAssistant/
│   │   │   │   │   │   ├── AIChat/
│   │   │   │   │   │   ├── AISearch/
│   │   │   │   │   │   └── AIRecommend/
│   │   │   │   │   ├── hooks/
│   │   │   │   │   ├── pages/
│   │   │   │   │   ├── services/
│   │   │   │   │   └── types/
│   │   │   │   │
│   │   │   │   ├── knowledge/         # 知识库模块
│   │   │   │   ├── organization/      # 组织管理模块
│   │   │   │   ├── notification/      # 通知模块
│   │   │   │   ├── calendar/          # 日历模块
│   │   │   │   ├── report/            # 报表模块
│   │   │   │   └── settings/          # 设置模块
│   │   │   │
│   │   │   ├── shared/                # 共享资源
│   │   │   │   ├── components/        # 通用组件
│   │   │   │   │   ├── Layout/
│   │   │   │   │   │   ├── AppLayout.tsx
│   │   │   │   │   │   ├── Sidebar.tsx
│   │   │   │   │   │   ├── Header.tsx
│   │   │   │   │   │   └── Footer.tsx
│   │   │   │   │   ├── PageHeader/
│   │   │   │   │   ├── EmptyState/
│   │   │   │   │   ├── LoadingState/
│   │   │   │   │   ├── ErrorBoundary/
│   │   │   │   │   ├── Modal/
│   │   │   │   │   ├── Drawer/
│   │   │   │   │   ├── Table/
│   │   │   │   │   ├── Form/
│   │   │   │   │   └── Charts/
│   │   │   │   ├── hooks/             # 通用hooks
│   │   │   │   │   ├── useDebounce.ts
│   │   │   │   │   ├── useThrottle.ts
│   │   │   │   │   ├── useLocalStorage.ts
│   │   │   │   │   ├── useMediaQuery.ts
│   │   │   │   │   └── useClickOutside.ts
│   │   │   │   ├── utils/             # 工具函数
│   │   │   │   │   ├── date.ts
│   │   │   │   │   ├── format.ts
│   │   │   │   │   ├── validation.ts
│   │   │   │   │   └── storage.ts
│   │   │   │   ├── constants/         # 常量定义
│   │   │   │   └── types/             # 全局类型
│   │   │   │
│   │   │   ├── styles/                # 全局样式
│   │   │   │   ├── globals.css
│   │   │   │   ├── variables.css
│   │   │   │   └── tailwind.css
│   │   │   │
│   │   │   └── main.tsx               # 入口文件
│   │   │
│   │   ├── index.html
│   │   ├── vite.config.ts
│   │   ├── tailwind.config.ts
│   │   ├── tsconfig.json
│   │   └── package.json
│   │
│   └── mobile/                        # 移动端应用（后期）
│
├── packages/                          # 共享包
│   ├── ui/                            # UI组件库
│   │   ├── src/
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   └── styles/
│   │   └── package.json
│   │
│   ├── utils/                         # 工具库
│   │   ├── src/
│   │   └── package.json
│   │
│   └── types/                         # 类型定义
│       ├── src/
│       └── package.json
│
├── services/                          # 后端服务
│   ├── gateway/                       # API网关
│   ├── auth-service/                  # 认证服务
│   ├── project-service/               # 项目服务
│   ├── task-service/                  # 任务服务
│   ├── ai-service/                    # AI服务
│   ├── knowledge-service/             # 知识库服务
│   └── notification-service/          # 通知服务
│
├── infrastructure/                    # 基础设施
│   ├── docker/
│   ├── k8s/
│   └── terraform/
│
├── docs/                              # 文档
├── scripts/                           # 脚本
├── pnpm-workspace.yaml                # Monorepo配置
├── turbo.json                         # Turborepo配置
└── package.json
```

### 2.3 状态管理架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              状态管理架构                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         全局状态 (Zustand)                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│  │  │  authStore  │  │  uiStore    │  │  appStore   │                 │   │
│  │  │  • user     │  │  • theme    │  │  • config   │                 │   │
│  │  │  • token    │  │  • sidebar  │  │  • features │                 │   │
│  │  │  • perms    │  │  • modal    │  │  • version  │                 │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    服务端状态 (TanStack Query)                       │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│  │  │  projects   │  │  tasks      │  │  knowledge  │                 │   │
│  │  │  • list     │  │  • list     │  │  • docs     │                 │   │
│  │  │  • detail   │  │  • tree     │  │  • search   │                 │   │
│  │  │  • members  │  │  • kanban   │  │  • graph    │                 │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│  │                                                                     │   │
│  │  特性：                                                             │   │
│  │  • 自动缓存和失效                                                   │   │
│  │  • 后台重新获取                                                     │   │
│  │  • 乐观更新                                                         │   │
│  │  • 无限滚动/分页                                                    │   │
│  │  • 离线支持                                                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       本地状态 (useState/useReducer)                 │   │
│  │  • 表单状态                                                         │   │
│  │  • UI交互状态                                                       │   │
│  │  • 临时数据                                                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.4 API设计

#### RESTful API规范

```typescript
// API响应格式
interface ApiResponse<T> {
  code: number          // 业务状态码
  message: string       // 提示信息
  data: T              // 响应数据
  timestamp: number    // 时间戳
  traceId: string      // 追踪ID
}

// 分页响应
interface PageResponse<T> {
  list: T[]
  total: number
  page: number
  pageSize: number
  hasMore: boolean
}

// API路径规范
// GET    /api/v1/projects              获取项目列表
// POST   /api/v1/projects              创建项目
// GET    /api/v1/projects/:id          获取项目详情
// PUT    /api/v1/projects/:id          更新项目
// DELETE /api/v1/projects/:id          删除项目
// GET    /api/v1/projects/:id/tasks    获取项目任务
// POST   /api/v1/projects/:id/tasks    创建任务
```

#### API服务封装

```typescript
// services/api/client.ts
import axios, { AxiosInstance, AxiosRequestConfig } from 'axios'
import { useAuthStore } from '@/modules/auth/store'

class ApiClient {
  private instance: AxiosInstance
  
  constructor() {
    this.instance = axios.create({
      baseURL: import.meta.env.VITE_API_BASE_URL,
      timeout: 30000,
      headers: {
        'Content-Type': 'application/json'
      }
    })
    
    this.setupInterceptors()
  }
  
  private setupInterceptors() {
    // 请求拦截
    this.instance.interceptors.request.use(
      (config) => {
        const token = useAuthStore.getState().token
        if (token) {
          config.headers.Authorization = `Bearer ${token}`
        }
        return config
      },
      (error) => Promise.reject(error)
    )
    
    // 响应拦截
    this.instance.interceptors.response.use(
      (response) => response.data,
      async (error) => {
        if (error.response?.status === 401) {
          useAuthStore.getState().logout()
          window.location.href = '/login'
        }
        return Promise.reject(error)
      }
    )
  }
  
  async get<T>(url: string, config?: AxiosRequestConfig): Promise<T> {
    return this.instance.get(url, config)
  }
  
  async post<T>(url: string, data?: any, config?: AxiosRequestConfig): Promise<T> {
    return this.instance.post(url, data, config)
  }
  
  async put<T>(url: string, data?: any, config?: AxiosRequestConfig): Promise<T> {
    return this.instance.put(url, data, config)
  }
  
  async delete<T>(url: string, config?: AxiosRequestConfig): Promise<T> {
    return this.instance.delete(url, config)
  }
}

export const apiClient = new ApiClient()
```

---

## 3. 核心功能设计

### 3.1 项目协作流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              项目协作流程                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 创建项目 │───▶│ AI分解  │───▶│ 智能分工 │───▶│ 执行追踪 │───▶│ 项目交付 │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│       │              │              │              │              │        │
│       ▼              ▼              ▼              ▼              ▼        │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 基本信息 │    │ 任务树  │    │ 成员匹配 │    │ 看板视图 │    │ 验收评审 │  │
│  │ 里程碑  │    │ 时间估算 │    │ 负载均衡 │    │ 进度汇总 │    │ 知识沉淀 │  │
│  │ 成员    │    │ 依赖关系 │    │ 技能匹配 │    │ 风险预警 │    │ 复盘总结 │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 任务层级结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              任务层级结构                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  项目 (Project)                                                             │
│  └── 里程碑 (Milestone)                                                     │
│      ├── 部门任务 (Department Task)                                         │
│      │   ├── 执行任务 (Task)                                                │
│      │   │   ├── 子任务 (Subtask)                                           │
│      │   │   └── 子任务 (Subtask)                                           │
│      │   └── 执行任务 (Task)                                                │
│      └── 部门任务 (Department Task)                                         │
│          └── 执行任务 (Task)                                                │
│                                                                             │
│  层级说明：                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Level 0: 项目 - 最高层级，包含项目元信息                           │   │
│  │  Level 1: 里程碑 - 项目阶段划分，有明确的时间节点                   │   │
│  │  Level 2: 部门任务 - 分配给特定部门的工作包                         │   │
│  │  Level 3: 执行任务 - 具体可执行的任务，分配给个人                   │   │
│  │  Level 4: 子任务 - 任务的细分步骤，可选                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 AI任务分解

```typescript
// modules/task/components/TaskDecompose/index.tsx
import { useState } from 'react'
import { useMutation } from '@tanstack/react-query'
import { Card, Input, Button, Tree, Space, message } from 'antd'
import { RobotOutlined, ThunderboltOutlined } from '@ant-design/icons'
import { taskApi } from '../../services/taskApi'
import type { TaskTree } from '../../types'
import { DecomposeResult } from './DecomposeResult'
import { TaskPreview } from './TaskPreview'
import styles from './index.module.css'

interface TaskDecomposeProps {
  projectId: string
  projectName: string
  onComplete: (tasks: TaskTree) => void
}

export function TaskDecompose({ projectId, projectName, onComplete }: TaskDecomposeProps) {
  const [description, setDescription] = useState('')
  const [taskTree, setTaskTree] = useState<TaskTree | null>(null)
  
  const decomposeMutation = useMutation({
    mutationFn: (desc: string) => taskApi.decomposeByAI(projectId, desc),
    onSuccess: (data) => {
      setTaskTree(data)
      message.success('任务分解完成')
    },
    onError: (error: Error) => {
      message.error(error.message || '分解失败')
    }
  })
  
  const createMutation = useMutation({
    mutationFn: (tree: TaskTree) => taskApi.createFromTree(projectId, tree),
    onSuccess: () => {
      message.success('任务创建成功')
      onComplete(taskTree!)
    }
  })
  
  const handleDecompose = () => {
    if (!description.trim()) {
      message.warning('请输入项目目标描述')
      return
    }
    decomposeMutation.mutate(description)
  }
  
  const handleConfirm = () => {
    if (taskTree) {
      createMutation.mutate(taskTree)
    }
  }
  
  const handleUpdateNode = (nodeId: string, updates: Partial<TaskTree>) => {
    // 更新任务树节点
    setTaskTree(prev => updateTreeNode(prev!, nodeId, updates))
  }
  
  return (
    <div className={styles.container}>
      {/* 输入区域 */}
      <Card className={styles.inputCard}>
        <div className={styles.header}>
          <RobotOutlined className={styles.icon} />
          <h3>AI智能任务分解</h3>
        </div>
        
        <p className={styles.tip}>
          描述您的项目目标和需求，AI将自动分解为可执行的任务结构
        </p>
        
        <Input.TextArea
          value={description}
          onChange={(e) => setDescription(e.target.value)}
          placeholder={`例如：${projectName}需要实现用户注册登录、商品展示、购物车、订单管理、支付功能...`}
          rows={4}
          maxLength={2000}
          showCount
          className={styles.textarea}
        />
        
        <div className={styles.actions}>
          <Button
            type="primary"
            icon={<ThunderboltOutlined />}
            onClick={handleDecompose}
            loading={decomposeMutation.isPending}
            size="large"
          >
            开始分解
          </Button>
        </div>
      </Card>
      
      {/* 分解结果 */}
      {taskTree && (
        <DecomposeResult
          taskTree={taskTree}
          onUpdateNode={handleUpdateNode}
          onConfirm={handleConfirm}
          onRegenerate={handleDecompose}
          loading={createMutation.isPending}
        />
      )}
    </div>
  )
}
```

### 3.4 智能分工

```typescript
// modules/task/components/TaskAssign/index.tsx
import { useState } from 'react'
import { useQuery, useMutation } from '@tanstack/react-query'
import { Card, List, Avatar, Tag, Button, Progress, Tooltip, message } from 'antd'
import { UserOutlined, BulbOutlined, CheckOutlined } from '@ant-design/icons'
import { taskApi } from '../../services/taskApi'
import type { Task, AssignSuggestion } from '../../types'
import styles from './index.module.css'

interface TaskAssignProps {
  task: Task
  onAssigned: () => void
}

export function TaskAssign({ task, onAssigned }: TaskAssignProps) {
  const [selectedMember, setSelectedMember] = useState<string | null>(null)
  
  // 获取分工建议
  const { data: suggestions, isLoading } = useQuery({
    queryKey: ['task-assign-suggestions', task.id],
    queryFn: () => taskApi.getAssignSuggestions(task.id)
  })
  
  // 分配任务
  const assignMutation = useMutation({
    mutationFn: (memberId: string) => taskApi.assign(task.id, memberId),
    onSuccess: () => {
      message.success('任务分配成功')
      onAssigned()
    }
  })
  
  const handleAssign = (memberId: string) => {
    setSelectedMember(memberId)
    assignMutation.mutate(memberId)
  }
  
  return (
    <Card 
      title={
        <Space>
          <BulbOutlined />
          <span>智能分工建议</span>
        </Space>
      }
      className={styles.card}
      loading={isLoading}
    >
      <div className={styles.taskInfo}>
        <h4>{task.name}</h4>
        <p>{task.description}</p>
        <Space>
          <Tag>预估: {task.estimatedHours}h</Tag>
          <Tag>技能: {task.requiredSkills?.join(', ')}</Tag>
        </Space>
      </div>
      
      <List
        dataSource={suggestions}
        renderItem={(item: AssignSuggestion) => (
          <List.Item
            className={styles.suggestionItem}
            actions={[
              <Button
                type={selectedMember === item.memberId ? 'primary' : 'default'}
                icon={selectedMember === item.memberId ? <CheckOutlined /> : null}
                onClick={() => handleAssign(item.memberId)}
                loading={assignMutation.isPending && selectedMember === item.memberId}
              >
                {selectedMember === item.memberId ? '已分配' : '分配'}
              </Button>
            ]}
          >
            <List.Item.Meta
              avatar={<Avatar src={item.avatar} icon={<UserOutlined />} />}
              title={
                <Space>
                  <span>{item.memberName}</span>
                  <Tag color="blue">{item.department}</Tag>
                  <Tag color="green">匹配度 {item.matchScore}%</Tag>
                </Space>
              }
              description={
                <div className={styles.memberInfo}>
                  <div className={styles.skills}>
                    技能: {item.skills.map(s => (
                      <Tag key={s} size="small">{s}</Tag>
                    ))}
                  </div>
                  <div className={styles.workload}>
                    <span>当前负载:</span>
                    <Progress 
                      percent={item.currentWorkload} 
                      size="small" 
                      status={item.currentWorkload > 80 ? 'exception' : 'normal'}
                      style={{ width: 100 }}
                    />
                  </div>
                  <div className={styles.reason}>
                    <Tooltip title={item.reason}>
                      <span>推荐理由: {item.reason.slice(0, 50)}...</span>
                    </Tooltip>
                  </div>
                </div>
              }
            />
          </List.Item>
        )}
      />
    </Card>
  )
}
```

### 3.5 任务看板

```typescript
// modules/task/components/TaskKanban/index.tsx
import { useState, useCallback } from 'react'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import {
  DndContext,
  DragOverlay,
  closestCorners,
  KeyboardSensor,
  PointerSensor,
  useSensor,
  useSensors,
  DragStartEvent,
  DragEndEvent
} from '@dnd-kit/core'
import { arrayMove, sortableKeyboardCoordinates } from '@dnd-kit/sortable'
import { taskApi } from '../../services/taskApi'
import type { Task, TaskStatus } from '../../types'
import { KanbanColumn } from './KanbanColumn'
import { KanbanCard } from './KanbanCard'
import styles from './index.module.css'

const COLUMNS: { id: TaskStatus; title: string; color: string }[] = [
  { id: 'pending', title: '待处理', color: '#8c8c8c' },
  { id: 'in_progress', title: '进行中', color: '#1890ff' },
  { id: 'review', title: '待审核', color: '#faad14' },
  { id: 'completed', title: '已完成', color: '#52c41a' }
]

interface TaskKanbanProps {
  projectId: string
  tasks: Task[]
  onTaskClick: (task: Task) => void
}

export function TaskKanban({ projectId, tasks, onTaskClick }: TaskKanbanProps) {
  const [activeTask, setActiveTask] = useState<Task | null>(null)
  const queryClient = useQueryClient()
  
  const sensors = useSensors(
    useSensor(PointerSensor, {
      activationConstraint: { distance: 8 }
    }),
    useSensor(KeyboardSensor, {
      coordinateGetter: sortableKeyboardCoordinates
    })
  )
  
  // 更新任务状态
  const updateStatusMutation = useMutation({
    mutationFn: ({ taskId, status }: { taskId: string; status: TaskStatus }) =>
      taskApi.updateStatus(taskId, status),
    onMutate: async ({ taskId, status }) => {
      // 乐观更新
      await queryClient.cancelQueries({ queryKey: ['tasks', projectId] })
      const previousTasks = queryClient.getQueryData(['tasks', projectId])
      
      queryClient.setQueryData(['tasks', projectId], (old: Task[]) =>
        old.map(t => t.id === taskId ? { ...t, status } : t)
      )
      
      return { previousTasks }
    },
    onError: (err, variables, context) => {
      // 回滚
      queryClient.setQueryData(['tasks', projectId], context?.previousTasks)
    },
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: ['tasks', projectId] })
    }
  })
  
  const handleDragStart = (event: DragStartEvent) => {
    const task = tasks.find(t => t.id === event.active.id)
    setActiveTask(task || null)
  }
  
  const handleDragEnd = (event: DragEndEvent) => {
    const { active, over } = event
    setActiveTask(null)
    
    if (!over) return
    
    const taskId = active.id as string
    const newStatus = over.id as TaskStatus
    const task = tasks.find(t => t.id === taskId)
    
    if (task && task.status !== newStatus) {
      updateStatusMutation.mutate({ taskId, status: newStatus })
    }
  }
  
  // 按状态分组任务
  const tasksByStatus = COLUMNS.reduce((acc, col) => {
    acc[col.id] = tasks.filter(t => t.status === col.id)
    return acc
  }, {} as Record<TaskStatus, Task[]>)
  
  return (
    <DndContext
      sensors={sensors}
      collisionDetection={closestCorners}
      onDragStart={handleDragStart}
      onDragEnd={handleDragEnd}
    >
      <div className={styles.kanban}>
        {COLUMNS.map(column => (
          <KanbanColumn
            key={column.id}
            id={column.id}
            title={column.title}
            color={column.color}
            count={tasksByStatus[column.id].length}
          >
            {tasksByStatus[column.id].map(task => (
              <KanbanCard
                key={task.id}
                task={task}
                onClick={() => onTaskClick(task)}
              />
            ))}
          </KanbanColumn>
        ))}
      </div>
      
      <DragOverlay>
        {activeTask && <KanbanCard task={activeTask} isDragging />}
      </DragOverlay>
    </DndContext>
  )
}
```

---

## 4. 实施计划

### 4.1 第一阶段：基础架构 + 核心协作（第1-4周）

#### 第1周：项目初始化

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 创建Monorepo项目结构 | P0 | 4h | 前端 |
| 配置Vite + TypeScript | P0 | 2h | 前端 |
| 配置Tailwind CSS | P0 | 2h | 前端 |
| 配置ESLint + Prettier | P0 | 2h | 前端 |
| 配置Husky + lint-staged | P1 | 1h | 前端 |
| 安装核心依赖 | P0 | 1h | 前端 |
| 创建目录结构 | P0 | 2h | 前端 |
| 配置路径别名 | P0 | 1h | 前端 |
| 创建全局样式变量 | P0 | 2h | 前端 |
| 创建主题配置 | P0 | 2h | 前端 |
| 后端项目初始化 | P0 | 4h | 后端 |
| 数据库设计 | P0 | 8h | 后端 |

#### 第2周：基础组件 + 布局

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| AppLayout组件 | P0 | 4h | 前端 |
| Sidebar组件 | P0 | 4h | 前端 |
| Header组件 | P0 | 3h | 前端 |
| PageHeader组件 | P0 | 2h | 前端 |
| EmptyState组件 | P0 | 2h | 前端 |
| LoadingState组件 | P0 | 2h | 前端 |
| ErrorBoundary组件 | P0 | 2h | 前端 |
| Modal封装 | P0 | 2h | 前端 |
| Drawer封装 | P0 | 2h | 前端 |
| 路由配置 | P0 | 3h | 前端 |
| API客户端封装 | P0 | 4h | 前端 |
| 认证API | P0 | 8h | 后端 |

#### 第3周：认证 + 项目模块

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 登录页面 | P0 | 4h | 前端 |
| 注册页面 | P0 | 3h | 前端 |
| 忘记密码页面 | P1 | 2h | 前端 |
| authStore实现 | P0 | 3h | 前端 |
| 路由守卫 | P0 | 2h | 前端 |
| ProjectCard组件 | P0 | 3h | 前端 |
| ProjectList组件 | P0 | 4h | 前端 |
| ProjectForm组件 | P0 | 6h | 前端 |
| ProjectDetail组件 | P0 | 6h | 前端 |
| projectStore实现 | P0 | 4h | 前端 |
| 项目CRUD API | P0 | 8h | 后端 |

#### 第4周：任务模块

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| TaskTree组件 | P0 | 8h | 前端 |
| TaskKanban组件 | P0 | 8h | 前端 |
| KanbanColumn组件 | P0 | 3h | 前端 |
| KanbanCard组件 | P0 | 3h | 前端 |
| TaskForm组件 | P0 | 4h | 前端 |
| TaskDetail组件 | P0 | 6h | 前端 |
| taskStore实现 | P0 | 4h | 前端 |
| 拖拽状态更新 | P0 | 4h | 前端 |
| 任务CRUD API | P0 | 8h | 后端 |
| 任务状态流转API | P0 | 4h | 后端 |

### 4.2 第二阶段：AI功能 + 知识管理（第5-8周）

#### 第5周：AI任务分解

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| TaskDecompose组件 | P0 | 8h | 前端 |
| DecomposeResult组件 | P0 | 4h | 前端 |
| TaskPreview组件 | P0 | 3h | 前端 |
| 分解结果编辑功能 | P0 | 4h | 前端 |
| AI分解API | P0 | 12h | 后端 |
| 分解提示词设计 | P0 | 8h | AI |
| 分解结果优化 | P1 | 4h | AI |

#### 第6周：智能分工

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| TaskAssign组件 | P0 | 6h | 前端 |
| AssignSuggestion组件 | P0 | 4h | 前端 |
| 成员技能标签 | P0 | 3h | 前端 |
| 负载可视化 | P0 | 3h | 前端 |
| 智能分工API | P0 | 12h | 后端 |
| 成员画像分析 | P0 | 8h | 后端 |
| 匹配算法优化 | P1 | 4h | AI |

#### 第7周：AI助手

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| AIAssistant组件 | P0 | 8h | 前端 |
| AIChat组件 | P0 | 6h | 前端 |
| 消息流式渲染 | P0 | 4h | 前端 |
| 快捷指令 | P0 | 3h | 前端 |
| AI对话API | P0 | 8h | 后端 |
| 上下文管理 | P0 | 4h | 后端 |
| 多模型切换 | P1 | 4h | 后端 |

#### 第8周：知识库

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 知识库列表页 | P0 | 4h | 前端 |
| 文档上传组件 | P0 | 4h | 前端 |
| 文档预览组件 | P0 | 4h | 前端 |
| 智能搜索组件 | P0 | 6h | 前端 |
| 知识图谱可视化 | P1 | 8h | 前端 |
| 文档解析服务 | P0 | 8h | 后端 |
| 向量化存储 | P0 | 6h | 后端 |
| RAG检索 | P0 | 8h | 后端 |

### 4.3 第三阶段：辅助功能 + 优化上线（第9-12周）

#### 第9周：组织管理 + 通知

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 部门管理页面 | P0 | 6h | 前端 |
| 成员管理页面 | P0 | 6h | 前端 |
| 角色权限管理 | P0 | 6h | 前端 |
| 通知中心组件 | P0 | 4h | 前端 |
| 通知列表页面 | P0 | 4h | 前端 |
| 组织管理API | P0 | 8h | 后端 |
| 通知服务 | P0 | 6h | 后端 |
| WebSocket推送 | P0 | 4h | 后端 |

#### 第10周：日历 + 报表

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 日历视图组件 | P0 | 8h | 前端 |
| 事件创建/编辑 | P0 | 4h | 前端 |
| 日程提醒 | P1 | 3h | 前端 |
| 报表Dashboard | P0 | 8h | 前端 |
| 图表组件封装 | P0 | 6h | 前端 |
| 数据导出 | P1 | 3h | 前端 |
| 日历API | P0 | 6h | 后端 |
| 报表统计API | P0 | 8h | 后端 |

#### 第11周：设置 + 优化

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 个人设置页面 | P0 | 4h | 前端 |
| 系统设置页面 | P0 | 4h | 前端 |
| 主题切换 | P0 | 3h | 前端 |
| 性能优化 | P0 | 8h | 前端 |
| 代码分割 | P0 | 4h | 前端 |
| 懒加载优化 | P0 | 4h | 前端 |
| 缓存优化 | P0 | 4h | 后端 |
| 接口优化 | P0 | 6h | 后端 |

#### 第12周：测试 + 上线

| 任务 | 优先级 | 工时 | 负责人 |
|------|--------|------|--------|
| 单元测试 | P0 | 8h | 前端 |
| 集成测试 | P0 | 8h | 测试 |
| E2E测试 | P0 | 8h | 测试 |
| 性能测试 | P0 | 4h | 测试 |
| Bug修复 | P0 | 12h | 全员 |
| 文档完善 | P1 | 4h | 前端 |
| 部署配置 | P0 | 4h | 运维 |
| 灰度发布 | P0 | 4h | 运维 |
| 监控配置 | P0 | 4h | 运维 |

---

## 5. 资源需求

### 5.1 人员配置

| 角色 | 人数 | 职责 | 参与周期 |
|------|------|------|----------|
| 前端开发 | 2-3 | 前端开发 | 全程 |
| 后端开发 | 2 | 后端开发 | 全程 |
| AI工程师 | 1 | AI功能开发 | 第5-8周 |
| 测试工程师 | 1 | 测试 | 第4周起 |
| UI设计师 | 1 | UI设计 | 第1-4周 |
| 产品经理 | 1 | 需求把控 | 全程 |

### 5.2 工时估算

| 阶段 | 前端工时 | 后端工时 | AI工时 | 测试工时 | 总计 |
|------|----------|----------|--------|----------|------|
| 第一阶段 | 200h | 80h | - | 20h | 300h |
| 第二阶段 | 160h | 120h | 40h | 40h | 360h |
| 第三阶段 | 120h | 60h | - | 60h | 240h |
| **总计** | **480h** | **260h** | **40h** | **120h** | **900h** |

### 5.3 成本估算

| 项目 | 单价 | 数量 | 费用 |
|------|------|------|------|
| 前端开发 | ¥800/天 | 60天 | ¥48,000 |
| 后端开发 | ¥800/天 | 33天 | ¥26,400 |
| AI工程师 | ¥1000/天 | 5天 | ¥5,000 |
| 测试工程师 | ¥600/天 | 15天 | ¥9,000 |
| UI设计师 | ¥600/天 | 20天 | ¥12,000 |
| 服务器/云服务 | - | - | ¥10,000 |
| AI API费用 | - | - | ¥5,000 |
| **总计** | - | - | **¥115,400** |

---

## 6. 风险管理

### 6.1 风险识别

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 开发进度延期 | 高 | 高 | 预留20%缓冲，每周评审 |
| 技术难点攻克 | 中 | 中 | 提前技术预研，备选方案 |
| AI效果不达预期 | 中 | 中 | 提供手动编辑，持续优化 |
| 人员变动 | 低 | 高 | 文档完善，知识共享 |
| 需求变更 | 中 | 中 | 敏捷开发，快速响应 |

### 6.2 里程碑检查点

| 检查点 | 时间 | 验收标准 |
|--------|------|----------|
| M1 | 第4周末 | 核心协作流程可用 |
| M2 | 第8周末 | AI功能可用 |
| M3 | 第10周末 | 全功能可用 |
| M4 | 第12周末 | 正式上线 |

---

## 7. 验收标准

### 7.1 功能验收

| 模块 | 验收标准 |
|------|----------|
| 项目管理 | 创建、编辑、删除、归档功能正常 |
| 任务管理 | 树形结构、看板、状态流转正常 |
| AI分解 | 分解成功率>80%，支持编辑 |
| 智能分工 | 推荐准确率>70% |
| 知识库 | 上传、检索、预览正常 |
| 通知 | 实时推送，已读未读正常 |

### 7.2 性能验收

| 指标 | 目标值 |
|------|--------|
| 首屏加载 | <1.5s |
| 页面切换 | <300ms |
| API响应 | <500ms (P95) |
| 并发用户 | >1000 |

### 7.3 代码质量

| 指标 | 目标值 |
|------|--------|
| 单文件行数 | <200行 |
| 测试覆盖率 | >80% |
| TypeScript覆盖 | 100% |
| ESLint错误 | 0 |

---

## 8. 附录

### 8.1 技术预研清单

| 技术 | 预研内容 | 时间 |
|------|----------|------|
| TanStack Query | 缓存策略、乐观更新 | 2天 |
| dnd-kit | 拖拽实现、性能优化 | 2天 |
| LangChain4j | AI编排、RAG实现 | 3天 |
| Milvus | 向量存储、检索优化 | 2天 |

### 8.2 参考资料

- [React 官方文档](https://react.dev/)
- [TanStack Query 文档](https://tanstack.com/query)
- [Zustand 文档](https://github.com/pmndrs/zustand)
- [dnd-kit 文档](https://dndkit.com/)
- [Tailwind CSS 文档](https://tailwindcss.com/)
- [LangChain4j 文档](https://docs.langchain4j.dev/)

---

*文档结束*