# Redisson配置文件
singleServerConfig:
  address: "redis://localhost:6379"
  database: 4
  connectionMinimumIdleSize: 1
  connectionPoolSize: 10
  connectTimeout: 10000
  timeout: 3000
  retryAttempts: 3
  retryInterval: 1500
  # 不设置password字段，表示不需要密码认证