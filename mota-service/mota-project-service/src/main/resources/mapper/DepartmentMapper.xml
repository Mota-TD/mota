<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mota.project.mapper.DepartmentMapper">

    <!-- 部门结果映射 -->
    <resultMap id="DepartmentResultMap" type="com.mota.project.entity.Department">
        <id column="id" property="id"/>
        <result column="org_id" property="orgId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="manager_id" property="managerId"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <!-- 根据组织ID查询部门列表 -->
    <select id="selectByOrgId" resultMap="DepartmentResultMap">
        SELECT d.*
        FROM department d
        WHERE d.org_id = #{orgId}
          AND d.deleted = 0
        ORDER BY d.sort_order ASC
    </select>

    <!-- 根据负责人ID查询部门列表 -->
    <select id="selectByManagerId" resultMap="DepartmentResultMap">
        SELECT d.*
        FROM department d
        WHERE d.manager_id = #{managerId}
          AND d.deleted = 0
        ORDER BY d.sort_order ASC
    </select>

</mapper>