# Mota Admin 重启指南

## 当前状态

正在重新安装 node_modules 依赖包，以解决 Umi 4.x 的 `.umi` 目录编译错误问题。

## 问题原因

Umi 4.x 使用 Mako 构建工具时，`.umi` 临时目录可能因为以下原因损坏：
1. 多个开发服务器同时运行
2. 构建过程中进程被强制终止
3. 文件系统缓存不一致

## 已执行的修复步骤

1. ✅ 杀死所有 Node.js 进程
2. ✅ 删除 `node_modules` 目录
3. ✅ 删除 `.umi` 临时目录
4. ✅ 删除 `package-lock.json` 锁文件
5. 🔄 正在执行 `npm install` 重新安装依赖

## 下一步操作

依赖安装完成后，执行：

```bash
cd mota-admin
npm run dev
```

开发服务器将在 `http://localhost:8000` 启动。

## Mock 登录测试

服务器启动后，您可以使用以下凭据测试登录：

- **用户名**: `admin`
- **密码**: 任意（已启用 Mock 数据）

Mock 配置文件位于：
- `mock/auth.mock.ts` - 认证相关接口
- `.env.development` - 环境变量配置（MOCK=enable）

## 常见问题

### 1. 如果启动失败

尝试清除缓存：
```bash
npm run setup
```

或完全重装：
```bash
rm -rf node_modules .umi package-lock.json
npm install
```

### 2. 如果 Mock 不生效

检查环境变量文件 `.env.development`：
```bash
MOCK=enable
```

### 3. 如果端口冲突

修改 `package.json` 中的端口号：
```json
"dev": "max dev --port 8001"
```

## 技术栈说明

- **框架**: Ant Design Pro 6.x
- **构建工具**: Umi 4.x (Mako)
- **UI组件库**: Ant Design 5.x
- **状态管理**: Umi 内置 Model
- **Mock工具**: Umi 内置 Mock 系统

## 项目结构

```
mota-admin/
├── src/
│   ├── pages/          # 页面组件
│   ├── services/       # API 服务
│   ├── components/     # 通用组件
│   └── models/         # 状态管理
├── mock/               # Mock 数据
├── config/             # Umi 配置
└── .env.development    # 开发环境变量
```

---

**注意**: 首次启动可能需要较长时间编译，请耐心等待。